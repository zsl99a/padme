---
type: rule
id: 005
status: active
last_updated: 2026-06-05
---

# 005 - 自动化执行约束

## 目的

兑现用户核心要求:「AI 或者通过简单的脚本可全自动执行」。

## 适用范围

所有进入评分阶段的机会。

## 具体规则

### 1. 自动化比例计算

```
auto_ratio = 自动化可完成的步骤数 / 总步骤数
```

- 步骤定义:从「启动」到「收到第一笔钱」中间所有可识别动作(如注册、发布、提交、跟踪)。
- 每个步骤必须能回答:「这一步是 AI 决定 + 脚本执行 / AI 生成 + 人工点击 / 全人工」。

### 2. 阈值

- `auto_ratio ≥ 0.8`:**通过**。
- `0.5 ≤ auto_ratio < 0.8`:**降级**为「半自动」,分数照打但档案必须标注「半自动」。
- `auto_ratio < 0.5`:**拒绝**。

### 3. 必备的「全自动最小栈」

通过的机会必须能映射到下列工具之一:

- **LLM 流水线**(Claude / GPT-4 + 工具调用 + API)。
- **现成 SaaS 组合**(Zapier / Make / n8n / Dify / Coze / Coze 空间 / Manus 类 Agent)。
- **自写脚本**(Python / Node + cron / GitHub Actions)。

### 4. 不可全自动化的典型场景(直接拒绝)

- 真人出镜拍摄(YouTube 真人频道、TikTok 真人)。
- 需要对方平台人工审核且无 API 通知。
- 需要实名且每账号绑定唯一身份证(无可绕过路径)。

### 5. 自动化成本计入启动成本

自动化栈本身的成本(API 月费、服务器)在评分「启动成本」维度里视为可接受开销,只要 < 预期月收入的 20%。

## 踩坑记录

- 2026-06-05:sub-agent 并行上限 8 验证有效,5,228 行报告在 ~2 小时内完成 — v3.0 验证 8 集群(cluster-1-wechat / cluster-2-creator-economy / cluster-3-ai-agent-freelance / cluster-4-ai-saas-api / cluster-5-ai-vertical-saas / cluster-6-platform-marketplace / cluster-7-ecommerce / cluster-8-ai-content-web3)同时派发,每个集群独立读 10-14 档案 + 002/003/008 规则 + 跑 v2.0 重评分;**并发 8 是合理上限**(8 集群 = 80 个机会全覆盖,集群边界清晰,资源不冲突);再多会因 Firecrawl 限流 / 上下文窗口竞争(001 §"集群划分"补充,write-v3-report/result.md §0)
- 2026-06-05:task.json 规范必须前置,8 集群全部按 task.json 模板创建 — 每个 sub-agent 任务文件 `subagent_tasks/<task-name>/task.json` 必填字段:任务 ID / 输入文件清单(8 集群验证文件 + 3 规则文件) / 输出文件路径 / 工具权限(firecrawl search+scrape / read_file) / 验证日期 / 评分标准;cluster-1..8 task.json 模板已成 SOP,后续 sub-agent 任务都应遵循(write-v3-report/result.md §0 + apply-rules/result.md §1)

## 变更日志

- 2026-06-04:初始建立。
- 2026-06-05:回写 v3.0 8 集群执行经验 — sub-agent 并行上限 8 验证有效;task.json 模板已成 SOP(001 协同)。
